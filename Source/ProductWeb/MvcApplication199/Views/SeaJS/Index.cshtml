@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <script src="/PlatJS/Scripts/seajs/sea-modules/seajs/1.3.0/sea-debug.js" type="text/javascript"></script>
    <script type="text/javascript">
        seajs.config({
            base: '/mvcapplication199/scripts/seajstest/src',
            alias: {
                'jquery': 'gallery/jquery/1.8.3/jquery'
            }
        })

        //define('console', function (require, exports) {
        //    exports.log = function (msg) {
        //        if (window.console && console.log) {
        //            console.log(msg);
        //        }
        //        else {
        //            alert(msg);
        //        }
        //    };
        //});

        seajs.use('index', function (fn) {
            fn.log('okgood');
        });
        //require('msgconsole').log('okgood');

        //msgconsole.log('okgood');

        //seajs.use('console', function(console){
        //    console.log('may i help you?!');
        //});

    </script>
</head>
<body>
    <div>
        
    </div>
</body>
</html>


@*seajs.use('console', function(console) {
    // now you can stop worrying about whether or not `console` is provided.
    // use it freely!
    console.log('hello world!');

    // well, we need to enhance the `console` module a bit.
    // for methods like console.warn, and method calls like console.log(msg1, msg2, msg3);
});*@